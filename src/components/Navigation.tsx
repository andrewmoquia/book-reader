import React from 'react';
import { Link } from 'react-router-dom';

export default function Navigation() {
   return (
      <nav id="nav_bar">
         <Link to="/">Home</Link>
      </nav>
   );
}
